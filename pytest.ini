[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
; Safety net: without this, a hung async test (e.g. an SSE/streaming
; endpoint that never terminates) blocks the whole suite indefinitely
; instead of failing fast.
timeout = 60
