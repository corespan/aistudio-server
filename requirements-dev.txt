# Development and testing dependencies — not needed in production containers.
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.24.0
anyio[trio]>=4.0.0
